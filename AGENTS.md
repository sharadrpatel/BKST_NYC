# Agent Instructions

Work only inside this repository.

Primary implementation reference:

- Read `docs/plan.md` before making changes.
- Follow `docs/plan.md` as the source of truth unless a deviation is necessary.
- If deviating, explain why before proceeding.

Project goals:

- Next.js App Router app
- Vercel deployment
- pre-assigned player ID access
- one scored attempt per ID
- leaderboard
- admin puzzle management

Rules:

- Do not create files outside this repo
- Keep business logic server-side where important
- Do not store secrets in code
- Use env vars for all credentials
- Prefer clean, production-minded architecture
